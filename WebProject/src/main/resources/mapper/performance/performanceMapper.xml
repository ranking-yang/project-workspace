<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.webproject.mapper.PerformanceMapper">
	<select id="getAllPerformances">
		SELECT * FROM performance
	</select>
	<select id="getPerformances">
		SELECT * FROM performance WHERE main_category=#{main_category}
	</select>
	<select id="getKidsPerformances">
		SELECT * FROM performance WHERE kid_state='Y'
	</select>
	<select id="getUserPerformancesforKids">
		SELECT * FROM performance 
		LEFT OUTER JOIN wish_list USING(performance_code) 
		WHERE kid_state = 'Y'
	</select>
	<select id="getUserPerformances">
		SELECT * FROM performance 
		LEFT OUTER JOIN wish_list USING(performance_code) 
		WHERE main_category=#{main_category}
	</select>
	<select id="getPerformance" resultType="PerformanceDTO">
		SELECT * FROM performance WHERE performance_code=#{performance_code}
	</select>
	
	
</mapper>